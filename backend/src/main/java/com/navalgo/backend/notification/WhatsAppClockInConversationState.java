package com.navalgo.backend.notification;

public enum WhatsAppClockInConversationState {
    AWAITING_ACTION,
    AWAITING_TIME,
    AWAITING_ADJUSTMENT_TIME,
    AWAITING_LOCATION,
    COMPLETED,
    EXPIRED
}
